Config = {} -- Don't Touch

Config.Coke = {
    Time = math.random(15, 15), -- Time it takes for harvest
    MarkCoords = vec3(-1977.06, 2.34, 122.5), -- Coords of field
    Amount = 1, -- The Amount of coke each harvest
    ItemFarm = "coca_leaf" -- Item given after each harvest
}